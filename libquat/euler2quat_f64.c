#include <stdint.h>
#include <math.h>
#include "quat.h"

quat_status euler2quat_f64(
	const euler_f64* pSrc, 
	quat_f64* pDst)
{
  quat_status status = QUAT_SUCCESS;

  return (status);
}
