#include <stdint.h>
#include <math.h>
#include "quat.h"

quat_status euler2quat_f32(
	const euler_f32* pSrc, 
	quat_f32* pDst)
{
  quat_status status = QUAT_SUCCESS;

  return (status);
}
